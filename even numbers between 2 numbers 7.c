#include<stduio.h>
#include<conio.h>
int main()
{
int a,i;
printf("Enter all the even numbers");
scanf("%d",&a)
printf("The even numbers are");
i=0;
while(i<n)
{
if(i%2)
}
printf("All the even numbeers are:");
getch();
return 0;
}
